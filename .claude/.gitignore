# .claude/ now holds only source: settings.json, toolkit-prompt.md, hooks/, modes/.
# Claude Code's own runtime state no longer lands here -- it lives in the container's
# dedicated persistent ~/.claude (see claude.py). So just ignore bytecode and any
# project-local settings a host session might drop here.
__pycache__/
*.pyc
settings.local.json
